- name: Overcloud Node Network Config
  hosts: allovercloud
  any_errors_fatal: true
  gather_facts: false
  tasks:
  - name: Install packages
    become: true
    dnf:
      name: "{{ item }}"
      state: present
    with_items:
      - openvswitch
      - os-net-config
